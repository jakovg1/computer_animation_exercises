using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class BackgroundObjectsMovement : MonoBehaviour
{ 
    //[SerializeField]
    //public float forwardMovementCoefficient = 2f;

    // Start is called before the first frame update
    void Start()
    {
        //var forwardVelocity = new Vector3(-forwardMovementCoefficient, 0, 0);
        //GetComponent<Rigidbody>().velocity = forwardVelocity;    
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
